@model Dierentuin.Models.Enclosure

@{
    ViewData["Title"] = "Create Enclosure";
}

<!-- Eventueel extra CSS -->
<style>
    body {
        background-color: #f1f3f5; /* Zachtgrijze achtergrond */
    }

    .page-header {
        background: linear-gradient(135deg, #007bff, #6c757d);
        color: #fff;
        padding: 2rem 1rem;
        border-radius: 8px;
        margin-bottom: 2rem;
        box-shadow: 0 3px 6px rgba(0, 0, 0, 0.1);
    }

        .page-header h1 {
            margin: 0;
            font-weight: 600;
        }

    .form-container {
        background-color: #fff;
        padding: 2rem;
        border-radius: 8px;
        box-shadow: 0 3px 6px rgba(0, 0, 0, 0.1);
    }

        .form-container label {
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .form-container input[type="text"],
        .form-container select {
            width: 100%;
            padding: 0.5rem;
            margin-bottom: 1rem;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .form-container input[type="submit"] {
            background-color: #007bff;
            color: #fff;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.2s;
        }

            .form-container input[type="submit"]:hover {
                background-color: #0056b3;
            }

        .form-container a {
            display: inline-block;
            margin-top: 1rem;
            color: #007bff;
            text-decoration: none;
            transition: color 0.2s;
        }

            .form-container a:hover {
                color: #0056b3;
            }
</style>

<div class="container mt-4">
    <!-- Header / Title -->
    <div class="page-header">
        <h1>Create Enclosure</h1>
    </div>

    <!-- Formulier -->
    <div class="form-container">
        <form asp-action="Create" method="post">
            <div class="mb-3">
                <label asp-for="Name" class="form-label"></label>
                <input asp-for="Name" class="form-control" />
            </div>
            <div class="mb-3">
                <label asp-for="Climate" class="form-label"></label>
                <select asp-for="Climate" class="form-select">
                    <option value="Tropical">Tropical</option>
                    <option value="Temperate">Temperate</option>
                    <option value="Arctic">Arctic</option>
                </select>
            </div>
            <div class="mb-3">
                <label asp-for="HabitatType" class="form-label"></label>
                <select asp-for="HabitatType" class="form-select">
                    <option value="Grassland">Grassland</option>
                    <option value="Forest">Forest</option>
                    <option value="Desert">Desert</option>
                </select>
            </div>
            <div class="mb-3">
                <label asp-for="SecurityLevel" class="form-label"></label>
                <select asp-for="SecurityLevel" class="form-select">
                    <option value="Low">Low</option>
                    <option value="Medium">Medium</option>
                    <option value="High">High</option>
                </select>
            </div>
            <div class="mb-3">
                <label asp-for="Size" class="form-label"></label>
                <input asp-for="Size" class="form-control" />
            </div>

            <input type="submit" value="Create" class="btn btn-primary" />
        </form>

        <p>
            <a asp-action="Index">Back to List</a>
        </p>
    </div>
</div>


